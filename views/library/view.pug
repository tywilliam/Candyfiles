extends ../layout

block content
        if viewfile

                    img.ui.large.centered.image(src=`/v/image/`+viewfile.filename)
                    .ui.header 
                        | #{viewfile.originalname}
                    .ui.segments
                        .ui.segment.center.aligned.red
                            p Download
                        .ui.segment.center.aligned.red
                            p Share
                        .ui.segment.center.aligned.green
                            p Copy URL



        else
                img.ui.small.image(src=`https://via.placeholder.com/300.png/09f/fff`)   
        script.
            console.log(#{viewfile});