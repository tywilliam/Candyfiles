extends ../layout

block content
  .container
    .row
      .col-md-12.text-center
          img.img-fluid.max-width(src=`/image/`+shortid)
          br
          br
          a.btn.btn-info(href=`/f/`+shortid)
                | Download
